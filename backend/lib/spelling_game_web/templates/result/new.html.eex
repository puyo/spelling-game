<h1>New Result</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.user_result_path(@conn, :create, @user_id)) %>

<span><%= link "Back", to: Routes.user_result_path(@conn, :index, @user_id) %></span>
